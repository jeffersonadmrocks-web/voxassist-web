# VoxAssist — Matriz Mestra de Configurações

Rastreamento vivo da reestruturação do menu Configurações em 9 áreas. Atualizado a cada rodada de trabalho — nunca reescrito do zero. Ver `docs internos`/conversa 2026-09-08 para o diagnóstico completo (5 agentes de pesquisa, schema.sql + migrations + grep completo do JS).

Princípio de execução: **corrigir o crítico + construir o seguro em paralelo**. Um item BLOQUEADO nunca bloqueia os demais.

Status possíveis: `PENDENTE` · `EM DIAGNÓSTICO` · `BLOQUEADO POR DEPENDÊNCIA` · `EM IMPLEMENTAÇÃO` · `IMPLEMENTADO` · `TESTADO` · `HOMOLOGADO`

---

## Conflitos estruturais críticos (prioridade imediata)

| # | Item | Classificação | Dependência | Ação | Status |
|---|---|---|---|---|---|
| C1 | Aviso falso "Não existe Loja/Unidade operacional" | CONSOLIDAR | nenhuma | Remover/corrigir texto em `company-only-mode-v0813.js` | **IMPLEMENTADO** (2026-09-08) |
| C2 | Funções sem migration versionada (`admin_soft_delete_user`, `admin_update_user_access_company_only`) | REVISAR | nenhuma pra documentar; bloqueia só alterações futuras nessas RPCs | Extraídas via `pg_get_functiondef`, documentadas em `20260908010000_document_admin_user_access_rpcs.sql` (corpo idêntico, zero mudança de comportamento). Achado extra: `admin_update_user_access_company_only` já grava `audit_log` -- corrige a suposição de "zero escritores" pra ações de usuário (continua zero pra OS/financeiro/estoque). | **IMPLEMENTADO** (2026-09-08) |
| C3 | 3 catálogos de chaves de permissão incompatíveis (`financeiro.*`×`finance.*`, `estoque.*`×`stock.*`, `config.*`×`settings.*`) gravando em `user_permissions` sem validação | REVISAR → CONSOLIDAR | nenhuma | Levantamento em produção: **nenhum dado real usa as chaves divergentes** (só o catálogo A já em uso). `user-access-management-v0813.js` corrigido pra usar as chaves canônicas -- sem migração de dado necessária. Endurecimento final (2026-09-08, migration `20260908200000`): função `is_valid_permission_key` (fonte única) + `admin_update_user_access`/`admin_update_user_access_company_only` agora rejeitam qualquer chave fora do catálogo. Achado ANTES de travar: a tela canônica (`user-access-management-v0813.js`) usa 24 chaves reais, 7 a mais que o recorte visual de `permissions-catalog-v0901.js` (os.print/os.financial/agenda.view_own/agenda.drag/financeiro.export/relatorios.export/config.companies) -- catálogo travado é a UNIÃO das 24, checado por grep nas 3 telas concorrentes + `select distinct` em `user_permissions` (zero linha existente fora do catálogo) antes de aplicar, pra garantir que travar não quebrava nenhuma gravação real. | **IMPLEMENTADO** (2026-09-08) |
| C4 | 4 seletores de "empresa ativa" concorrentes (`company-only-mode`, `company-selector-singleton`, `user-logoff`, resíduo em `user-permissions-ui`) | CONSOLIDAR | nenhuma pra outras áreas | Investigação (2026-09-09) confirmou por leitura comparativa dos 4 arquivos + ordem de carregamento real no `index.html`: `company-selector-singleton-v0813.js` já era a fonte que de fato aparecia pro usuário -- nomeado "seletor único" de propósito, carrega POR ÚLTIMO dos 4 (ordem real do script), e sua `hideAllLegacy()` já escondia via CSS/JS os 4 IDs dos outros 3 (inclusive o do próprio `company-only-mode`). O que faltava era só remover o código morto (fetch+`<select>`+handler duplicados, escondidos mas ainda rodando) dos outros 3 e expor `window.vxRefreshCompanySelector` pra quem trocar de empresa por outro caminho (ex.: botão "usar esta empresa" da lista de empresas) pedir a atualização do único seletor real -- corrige de quebra um bug latente (rótulo do seletor ficava desatualizado até a próxima mutação do DOM nesses casos). Nenhuma tabela/RLS tocada -- só limpeza de JS de UI. `doLogout`/botão SAIR de `user-logoff-v0813.js`/`user-permissions-ui-v0813.js` (responsabilidade diferente, fora do escopo) preservados intocados. | **IMPLEMENTADO** (2026-09-09) |
| C5 | 3 telas "Alterar Usuário" concorrentes no mesmo botão (`company-only-mode`, `user-permissions-ui`, `user-access-management`) | CONSOLIDAR | bloqueia só polimento de Área 01 | O que já existia: as 3 telas editavam o MESMO usuário/tabela por caminhos diferentes. Achado antes de consolidar: `user-permissions-ui-v0813.js` tinha um interceptador de clique em fase de CAPTURA (`stopImmediatePropagation`) que na prática SEMPRE vencia a concorrência pelos botões `data-user-manage` de `company-only-mode` -- e tanto ele quanto `company-only-mode` tinham uma capacidade real que `user-access-management` NÃO tinha: "Empresas Liberadas" (via Edge Function já existente `voxassist-manage-user`, `action:set_companies`). O que foi reaproveitado: a MESMA Edge Function, mesmo contrato, portada pro modal de `user-access-management-v0813.js` (`managedCompanies()`+seção condicional, só aparece se o gestor administra mais de 1 empresa). O que foi removido/consolidado: `manageUser()` de `company-only-mode-v0813.js` (dead code, função + constante `perms` não usada mais); `fetchManagedCompanies/fetchUser/permissionCard/openPermissions` + o interceptador de clique de `user-permissions-ui-v0813.js` (dead code, + `roleOptions/accessOptions/permissionGroups/uid/activeCompany` órfãos removidos). O que foi criado: nada de novo no backend -- só `window.vxRefreshUsersTable` (exportado por `user-access-management-v0813.js`), chamado deterministicamente por `company-only-mode-v0813.js` logo após `renderAdmin()` (mesmo princípio do C4: elimina a corrida por timing/MutationObserver, `user-access-management-v0813.js` passa a ser SEMPRE quem renderiza a tabela real). `newUser`/`+ CADASTRAR USUÁRIO`/lista de empresas de `company-only-mode` preservados intocados (fora do escopo do C5, é criação de usuário, não "Alterar"). | **IMPLEMENTADO** (2026-09-09) |
| C6 | 2 formulários de cadastro/edição de empresa com cobertura de campos diferente | CONSOLIDAR | nenhuma pra outras áreas | Achado ao investigar (mesma classe do C5): `company-profile-complete-v0812.js` (fiscal, logo, horário) JÁ vencia na prática -- interceptava o clique em `.vx-edit-company` em fase de CAPTURA (`stopImmediatePropagation`) antes do `onclick` normal de `company-guard-confirm-edit-v0812.js` (que chamava sua própria versão simples, `editCompany()`, nunca alcançada). Resolvido: exposto `window.vxEditCompanyFull`, o criador real do botão (`enhanceAdmin()` em `company-guard-confirm-edit-v0812.js`) agora chama direto, sem depender da corrida de listeners; `editCompany()` (dead code) removida. `applyGlobalGestorView()` de `company-management-final-v0813.js` também removida (já autoconfirmada como código morto por um comentário do usuário de 2026-09-07). `editStore()`/loja (mesmo arquivo) preservado intocado -- sem concorrente, fora do escopo. Achado adicional fora do escopo estrito do C6, registrado pra limpeza futura: `company-hierarchy-v0813.js` e `company-multilist-fix-v0813.js` têm o mesmo guard `.vx-admin-grid` morto (nunca casa com o DOM real) -- inertes hoje, não removidos nesta rodada. | **IMPLEMENTADO** (2026-09-09) |

---

## Área 01 — Empresa & Usuários

| Item | Classificação | Ação | Migration | Arquivos | Status |
|---|---|---|---|---|---|
| Empresas (razão social, CNPJ, endereço, logo, docs) | REAPROVEITAR (C6 fechado) | — | — | `company-profile-complete-v0812.js` (única fonte real agora) | IMPLEMENTADO |
| Unidades/lojas + ativar/desativar | REAPROVEITAR | — | `20260907020000_admin_upsert_store.sql` | `service-stores-admin-v0907.js` | IMPLEMENTADO |
| Parâmetros próprios por unidade | CRIAR | — | — | — | PENDENTE |
| Usuários (cadastro/gerenciamento) | REAPROVEITAR (C5 fechado) | — | — | `user-access-management-v0813.js` (única tela real agora) | IMPLEMENTADO |
| Perfis | REAPROVEITAR | Decisão do usuário (2026-09-09): manter os 5 perfis reais do banco (GESTOR/ATENDENTE/TECNICO/ESTOQUE/FINANCEIRO) -- a especificação original citava "Administrativo"/"Personalizado" em vez de Estoque/Financeiro, mas o banco já evoluiu diferente e está certo. Não criar 6º perfil; flexibilidade fica nas permissões individuais (ver C3, já travado). Nenhuma alteração de schema. | — | `profiles_role_check` | **FECHADO** (2026-09-09) |
| Permissões por módulo/ação | REAPROVEITAR (C3 e C5 fechados) | C3 (catálogo travado no servidor) e C5 (tela única) resolvidos | — | — | IMPLEMENTADO |
| Técnicos: interno/externo, agenda | REAPROVEITAR | — | — | `profiles.external_schedule_enabled` | IMPLEMENTADO |
| Técnicos: região, especialidade, disponibilidade | CRIAR | Decisão do usuário (2026-09-09): região = associação com `service_regions` (catálogo já existente); especialidade = NÃO texto livre, reaproveita `product_types` (catálogo mestre GLOBAL da Área 03) como vocabulário -- sem inventar dicionário novo; disponibilidade = 1 linha por dia da semana por técnico, mesmas chaves de dia de `companies.business_hours`. 3 tabelas novas (`technician_regions`/`technician_specialties`/`technician_availability`, migration `20260909010000`) + 3 RPCs gestor-only (`admin_set_technician_regions`/`_specialties`/`_availability`). Card "TÉCNICOS — REGIÃO, ESPECIALIDADE E DISPONIBILIDADE" em `settings-schedule-v0907.js` (Área 04, mesma tela dos outros cards de técnico/agenda -- categorizado na Área 01 na Matriz original, mas fisicamente vive na tela de Agenda por ser onde é usado, mesmo precedente de "Técnicos disponíveis na agenda"). Escopo: cadastro + exibição -- roteamento automático por essas regras fica pra evolução futura. | `20260909010000` | `settings-schedule-v0907.js` | **IMPLEMENTADO** (2026-09-09) |
| Segurança: redefinir senha (gestor→usuário) | REAPROVEITAR (endpoint público) | Decisão do usuário (2026-09-09), opção B: gestor solicita, usuário define -- nunca o contrário. Confirmado por leitura do código-fonte oficial de `@supabase/auth-js` (não por suposição) que `resetPasswordForEmail` é só `POST /auth/v1/recover` com `{email}` -- MESMO endpoint público de "esqueci minha senha", sem precisar de Edge Function/service_role. Reaproveita o helper `auth()` já existente em `app.js`. Botão "REDEFINIR SENHA" no modal de Alterar Usuário (`user-access-management-v0813.js`). Lado de recebimento novo: `password-reset-v0909.js` detecta o link de recuperação (`#access_token=...&type=recovery`) e mostra tela de definir nova senha, `PUT /auth/v1/user` com o token do link. Risco registrado, não testado ponta a ponta: se o projeto usa fluxo PKCE por e-mail (não confirmável sem acesso ao painel do Supabase Auth), o link viria por `?code=` em vez de `#access_token=` e a troca exigiria um `code_verifier` que só existe no navegador de quem SOLICITOU (o gestor, não o usuário-alvo) -- limite inerente do PKCE, tratado com mensagem clara em vez de travar silenciosamente. | `password-reset-v0909.js`, sem migration | `password-reset-v0909.js`, `user-access-management-v0813.js` | **IMPLEMENTADO -- pendente teste manual ponta a ponta** (2026-09-09) |
| Segurança: encerrar sessão remota | CRIAR (RPC, sem Edge Function) | Decisão do usuário (2026-09-09): implementar, "sem endpoint genérico excessivamente privilegiado". Confirmado por leitura de código-fonte (`@supabase/auth-js`) que `auth.admin.signOut` exige o JWT da sessão-alvo (não um `user_id`) -- inviável pro gestor. Mecanismo real por trás: `delete from auth.sessions where user_id=...` -- `auth.refresh_tokens` tem FK `ON DELETE CASCADE` pra `auth.sessions` (confirmado via `pg_constraint`, testado acesso de leitura real antes de aplicar). RPC `admin_terminate_user_sessions` (migration `20260909030000`, security definer, gestor-only + usuário vinculado à empresa, audita em `audit_log`). Botão "ENCERRAR SESSÕES REMOTAS" no mesmo modal. Access token corrente de cada sessão continua válido até expirar sozinho (limite do próprio Supabase, não desta implementação). | `20260909030000` | `user-access-management-v0813.js` | **IMPLEMENTADO** (2026-09-09) |
| Segurança: bloqueio/desbloqueio de conta | REAPROVEITAR | Decisão do usuário (2026-09-09): bloquear = desativar acesso (`profiles.active`/`user_companies.active`, já existente) -- sem mecanismo automático por tentativa de senha (evolução futura). Rótulo do checkbox em `user-access-management-v0813.js` trocado de "USUÁRIO ATIVO" pra texto dinâmico "ACESSO ATIVO -- desmarque para desativar" / "ACESSO DESATIVADO -- marque para reativar", deixando a ação clara pro gestor. | — | `user-access-management-v0813.js` | **FECHADO** (2026-09-09) |
| Histórico básico de acesso (login/logout) | CRIAR | Decisão do usuário (2026-09-09): registro simples agora (usuário, empresa, data/hora, evento, user_agent quando disponível), sem mexer no schema do Supabase Auth. `access_history` (migration `20260909020000`, FK direta pra `profiles` pra permitir embed do nome), insert-only pelo próprio usuário, leitura só gestor. Interceptado num único ponto (`window.auth`/`window.loadProfile` empacotados em `access-history-v0909.js`) -- LOGIN só em autenticação real por senha (não em restore de sessão ao recarregar), LOGOUT em qualquer chamada de logout (cobre os 3 pontos existentes sem editar nenhum deles). Card "HISTÓRICO DE ACESSO" em `settings-security-v0908.js`. `audit_log` continua cobrindo só alteração de cadastro, conceito diferente, não duplicado. | `20260909020000` | `access-history-v0909.js` | **IMPLEMENTADO** (2026-09-09) |

## Área 02 — Ordens de Serviço

| Item | Classificação | Ação | Status |
|---|---|---|---|
| Numeração | REAPROVEITAR | Só expor leitura em Configurações. Card informativo "NUMERAÇÃO" em `settings-order-types-v0908.js`, regra fixa de `os-number-format-v0812.js` (nenhuma lógica nova). | **IMPLEMENTADO** (2026-09-08) |
| Motor de fluxo/status automático | REAPROVEITAR — **nunca recriar** | `advance_service_order_status` é fonte única | IMPLEMENTADO (é o motor existente) |
| Rótulos de status duplicados (`manual-status-v0812.js` FLOW × `vxOsStatusLabel`) | CONSOLIDAR | Revisado 2026-09-08: `labelOf()` de manual-status-v0812.js já PREFERE `window.vxOsStatusLabel` sempre que disponível (`os-status-engine-v0903.js` carrega antes) -- na prática `FLOW` só serve de fallback morto pro rótulo. Risco baixo (rótulo já é o certo hoje). `FLOW` continua com um uso real, diferente: é a lista ORDENADA de status oferecida no seletor manual de situação -- essa parte não é redundante, fica pra depois com menor prioridade. | BAIXA PRIORIDADE (não é bug funcional) |
| Tipos de OS (ativar/desativar sem apagar histórico) | CRIAR | `order_types` por empresa (migration `20260908050000`) + RPC `admin_upsert_order_type`, gestor-only. Empresas existentes semeadas com os 5 tipos já em uso; empresa nova semeada por trigger. Tela própria `settings-order-types-v0908.js`. `order-type-v0812.js` (Nova OS) já lê desta tabela, fallback pra lista fixa se vazia. GARANTIA/REINGRESSO mantidos com os mesmos nomes (comportamento especial por string exata, aviso na tela). | **IMPLEMENTADO** (2026-09-08) |
| Tipo de atendimento (Interno/Externo) | REAPROVEITAR | Decisão do usuário (2026-09-09): sem requisito funcional real pra um 3º valor -- a nota anterior ("3º valor exige migration") não tinha um caso de uso por trás, foi corrigida. Continua `INTERNO`/`EXTERNO`, sem migration. | **FECHADO** (2026-09-09) |
| Campos obrigatórios por etapa | CRIAR (exposição) | Regra já existe no motor SQL (`compute_missing_for_status`, migration `20260903050000`), falta só exibir -- card informativo "CAMPOS OBRIGATÓRIOS POR ETAPA" em `settings-order-types-v0908.js`, texto espelha 1:1 as condições da função SQL, sem duplicar lógica nova. | **IMPLEMENTADO** (2026-09-08) |
| Termos e condições (por tipo de documento, versionado) | CRIAR | Confirmado: nunca implementado, nenhum artefato no repo. Decisão do usuário (2026-09-09): aprovado o plano completo "Arquitetura de Documentos da OS" (25 seções), execução por fases começando pela Fase 1 -- ver `elegant-honking-raven.md`. | **APROVADO -- EM IMPLEMENTAÇÃO (Fase 1)** |
| Impressão e documentos (seleção/parametrização de modelos) | CONSOLIDAR + CRIAR | Duplicação real de HTML entre impressão e WhatsApp -- faz parte do mesmo plano aprovado acima (unificação Whirlpool é pré-requisito da Fase 1) | **APROVADO -- EM IMPLEMENTAÇÃO (Fase 1)** |
| Importação/exportação (reclassificado da Área 09, 2026-09-09) | REVISAR | Hoje é sempre específico de OS (Whirlpool/Electrolux) -- pertence aqui, não a Sistema & Segurança. Nenhuma unificação genérica pedida; fica como referência até haver necessidade real. | EM DIAGNÓSTICO |

## Área 03 — Cadastros & Catálogos

| Item | Classificação | Status |
|---|---|---|
| Grupos e tipos de produto (TV/Geladeira/Freezer/...) | **REAPROVEITAR catálogo mestre (intocado) + CRIAR camada de associação por empresa** | **IMPLEMENTADO** (2026-09-08). Catálogo mestre `product_groups`/`product_types` confirmado global, seeded, sem FK externa, sem trigger/função, sem policy de escrita -- decisão do usuário: nunca duplicar por empresa, nunca dar escrita direta nele. Criada `company_product_types` (migration `20260908030000`) + RPC `admin_set_company_product_type` (gestor-only; ausência de linha = ativo por padrão, empresa herda o catálogo inteiro sem reconfigurar nada). Tela própria `settings-product-catalog-v0908.js` -- primeira área a sair da página única e virar destino próprio no hub. `inferGroup()` (os-detail-v0812.js) mantido como está (2 usos, só exibição, nunca persistido) -- consolidação com o catálogo real fica pra depois de mapear todos os consumidores, sem apagar o fallback ainda. |
| Defeitos | CRIAR | **IMPLEMENTADO catálogo** (2026-09-08) -- `product_defects` por empresa (migration `20260908070000`), RPC `admin_upsert_product_defect`, card em `settings-product-catalog-v0908.js`. Sem seed (campo era texto livre, nada pra preservar). Ligação com o campo DEFEITO RELATADO (autocomplete/sugestão) fica pra depois -- campo continua texto livre por ora. |
| Estado do produto | CRIAR | **IMPLEMENTADO** (2026-09-08) -- `product_conditions` por empresa (migration `20260908060000`), RPC `admin_upsert_product_condition`, card na mesma tela de Produtos (`settings-product-catalog-v0908.js`). Empresas existentes semeadas com NOVO/USADO/ARRANHADO/AVARIADO (nada perdido); gestor livre pra renomear/adicionar. Ligado ao campo ESTADO DO APARELHO na Nova OS e na aba Equipamento da OS aberta, com fallback pra lista fixa. |
| Acessórios | CRIAR | **IMPLEMENTADO catálogo** (2026-09-08) -- `product_accessories` por empresa (migration `20260908070000`), RPC `admin_upsert_product_accessory`, card em `settings-product-catalog-v0908.js`. Sem seed. Ligação com o campo ACESSÓRIOS fica pra depois -- campo continua texto livre por ora. |
| Serviços com valor padrão | CRIAR | **IMPLEMENTADO catálogo** (2026-09-08) -- `services_catalog` por empresa (migration `20260908070000`), RPC `admin_upsert_service_catalog_item` (nome + valor padrão), card em `settings-product-catalog-v0908.js`. Sem seed. Ligação com o orçamento da OS fica pra depois -- valor continua digitado livremente por ora. |

⚠️ "Grupos de Atendimento" (já existe) é conceito diferente (roteamento interno), não confundir com catálogo de produto.

## Área 04 — Agenda & Atendimento

| Item | Classificação | Status |
|---|---|---|
| Horários (dias/período/capacidade da empresa) | REAPROVEITAR | IMPLEMENTADO |
| Técnicos disponíveis na agenda | CONSOLIDAR | **IMPLEMENTADO** (2026-09-08) -- achado: `profiles.external_schedule_enabled` já era lido em vários lugares (field-agenda-complete-v0813.js, dashboard, electrolux-agenda-bridge) mas nunca tinha nenhum jeito de ESCREVER pelo app (zero writers confirmado). RPC nova `admin_set_technician_external_schedule` (gestor-only, migration `20260908080000`) + card novo em `settings-schedule-v0907.js` (mesma tela de Horários), lista técnicos com checkbox. Não duplica cadastro de técnico. |
| Capacidade por técnico/região | CRIAR | PENDENTE (futuro, não urgente) |
| Regiões de atendimento | CRIAR | **IMPLEMENTADO catálogo** (2026-09-08) -- `service_regions` por empresa (migration `20260908090000`), card em `settings-schedule-v0907.js` (mesma tela de Agenda). Associar região a técnico fica pra depois. |
| Conflito/transferência/sem técnico definido | REAPROVEITAR | IMPLEMENTADO -- ver detalhe em "Alertas" logo abaixo (conflito de sobreposição era o único caso genuinamente faltante, corrigido em 2026-09-09) |
| Períodos disponíveis (enum) | REAPROVEITAR | Decisão do usuário (2026-09-09): manter `HORARIO_COMERCIAL`/`HORARIO_ESPECIFICO` no CHECK mesmo sem uso hoje -- reservados pra exceções/evolução da Agenda, remover só depois da consolidação completa da área. Nenhuma alteração de schema. | **FECHADO** (2026-09-09) |
| Alertas (parâmetros da regra) | CRIAR + REAPROVEITAR | Decisão do usuário (2026-09-09): 4 casos -- (1) conflito/sobreposição: NÃO existia nenhuma checagem, 2 OS podiam cair no mesmo técnico+dia+período sem aviso -- `hasConflict()` nova em `field-agenda-complete-v0813.js`, mesmo padrão não-bloqueante de `isBlocked` (confirm() antes de prosseguir). Achado extra: `openNewForOrder` nem checava bloqueio/feriado (só `openEditor` checava) -- corrigido junto, mesmo padrão nos 2 formulários agora. (2) técnico indisponível: já existia (`isBlocked`, bloqueios manuais + feriado), agora aplicado nos 2 formulários. (3) agendamento sem técnico: já existia e já é visível -- faixa "🟨 Agendamentos em aberto" no topo da Agenda, com contador. (4) card de atenção: já existia (`important_alert`/`editAttention()`), simples e visível pra equipe, conforme já definido pelo usuário -- nenhuma mudança. Atraso/check-in fica pra quando o fluxo de execução em campo estiver mais definido (explicitamente adiado pelo usuário). | **IMPLEMENTADO** (2026-09-09) |

## Área 05 — Estoque & Peças

| Item | Classificação | Status |
|---|---|---|
| Locais de estoque (múltiplos depósitos) | CRIAR | **IMPLEMENTADO catálogo** (2026-09-08) -- `stock_locations` por empresa (migration `20260908090000`), tela própria nova `settings-stock-v0908.js` (primeiro conteúdo real da Área 05, saiu de placeholder). Separar saldo de `stock_items` por local fica pra depois. |
| Categorias de peças | CRIAR | **IMPLEMENTADO catálogo** (2026-09-08) -- `part_categories`, mesma tela de Estoque. |
| Unidades (UN/KIT/PAR/METRO) | CRIAR | **IMPLEMENTADO catálogo** (2026-09-08) -- `stock_units`, mesma tela de Estoque. |
| Movimentações | CONSOLIDAR (schema existe, zero gravação) | PENDENTE |
| Estoque técnico | REAPROVEITAR schema / CRIAR gravação | PENDENTE |
| Fabricantes (garantia/reembolso) | CRIAR | **IMPLEMENTADO catálogo** (2026-09-08) -- `manufacturers` por empresa (migration `20260908110000`, nome + garantia padrão em dias + observação de reembolso), card em `settings-stock-v0908.js` (mesma tela de Estoque). Sem seed. Ligação com o campo MARCA da Nova OS ou com o cálculo de garantia por OS fica pra depois. |
| Alertas | CRIAR | **IMPLEMENTADO cadastro** (2026-09-08) -- `stock_items.min_quantity` (limite por peça, opcional) + `companies.stock_alert_default_min_quantity` (padrão da empresa), migration `20260908180000`. Card "ALERTA DE ESTOQUE BAIXO" em `settings-stock-v0908.js`: parâmetro padrão + contagem real de peças abaixo do limite hoje (leitura). Editar o limite de uma peça específica continua na tela de Estoque operacional (não tocada); nenhuma notificação é disparada ainda -- só cadastro + contagem. |

## Área 06 — Financeiro

| Item | Classificação | Status |
|---|---|---|
| Formas de pagamento (ativar/ordem) | CRIAR | **IMPLEMENTADO** (2026-09-08) -- tabela `payment_methods` por empresa (migration `20260908040000`), RPC `admin_upsert_payment_method`, tela própria `settings-payment-methods-v0908.js`. Empresas existentes semeadas com as 7 formas já em uso (nada perdido); empresa nova semeada por trigger. Guia Finalizar OS (`os-detail-v0812.js`) já lê desta tabela, com fallback pra lista fixa se a empresa não tiver nenhuma. "DESCONTO" continua com o mesmo nome (comparação por string no frontend depende disso). |
| Parcelamento configurável | CRIAR | **IMPLEMENTADO cadastro** (2026-09-08) -- `payment_methods.max_installments` (migration `20260908130000`), RPC `admin_upsert_payment_method` estendida (parâmetro novo com default, retrocompatível) para gravar/limpar o limite. Campo no mesmo modal de "Renomear forma de pagamento". Ainda não validado no formulário de Finalizar OS -- só cadastro. |
| Contas e caixas | CRIAR | **IMPLEMENTADO catálogo** (2026-09-08) -- `cash_accounts` por empresa (migration `20260908090000`), card em `settings-payment-methods-v0908.js` (mesma tela de Financeiro). Vincular pagamento a uma conta específica fica pra depois. |
| Categorias financeiras | CRIAR | **IMPLEMENTADO catálogo** (2026-09-08) -- `financial_categories`, mesma tela de Financeiro. |
| Regras de recebimento | REAPROVEITAR | Decisão do usuário (2026-09-09): requisito principal é "OS não pode ser encerrada/entregue com inconsistência financeira" -- validado que já está implementado desde 2026-09-07 (`compute_next_service_order_status`, migration `20260907050000`): FINALIZADA só é alcançada com `p_paid_total >= p_full_budget_total`, motor único, sem duplicação. Sem política obrigatória de sinal percentual (não pedida). Evoluções (baixa manual, desconto, estorno, sinal) ficam separadas, sem escopo definido ainda. | **FECHADO** (2026-09-09) |
| Descontos (limite/autorização por perfil) | CRIAR | **IMPLEMENTADO cadastro** (2026-09-08) -- `discount_limits` (migration `20260908140000`, 1 linha por (empresa, perfil fixo GESTOR/ATENDENTE/TECNICO), RPC `admin_set_discount_limit` gestor-only). Card "LIMITE DE DESCONTO POR PERFIL" em `settings-payment-methods-v0908.js`. Ainda não validado quando a forma DESCONTO é lançada na guia Finalizar OS -- só cadastro. |
| Parâmetros (juros/taxas/arredondamento) | CRIAR | **IMPLEMENTADO cadastro** (2026-09-08) -- 3 colunas novas em `companies` (migration `20260908120000`: juros ao mês %, multa %, modo de arredondamento), editadas por PATCH direto (mesmo padrão de `document_footer`/`business_hours`, protegido pela policy `companies_update_gestor` já existente, sem RPC nova). Card "PARÂMETROS FINANCEIROS" em `settings-payment-methods-v0908.js`. Ainda não aplicado em nenhum cálculo da guia Finalizar OS -- só cadastro. |

## Área 07 — Comunicação & Automação

| Item | Classificação | Status |
|---|---|---|
| Canais (WhatsApp/Chat) | REAPROVEITAR — **não tocar na base** | IMPLEMENTADO (referência apenas) |
| Horário de atendimento | CONSOLIDAR com tabelas de Agenda | **IMPLEMENTADO (consolidação por referência)** (2026-09-08) -- achado: não existe (e não precisa existir) um horário separado pra Comunicação -- é o mesmo `companies.business_hours` já usado pela Área 04. Em vez de duplicar tela/tabela, card "HORÁRIO DE ATENDIMENTO" em `settings-communication-v0908.js` explica isso e linka pra Agenda & Atendimento (mesma tela real, sem nova). |
| Mensagens padrão / variáveis | CRIAR (coluna morta encontrada, não reaproveitar) | **IMPLEMENTADO** -- cadastro de nome+texto por empresa (migration `20260908100000`, RPC `admin_upsert_message_template`), tela própria `settings-communication-v0908.js`, primeiro conteúdo real da Área 07 (hub saiu de 'admin' pra 'comunicacao'). Escopo só cadastro: não dispara nada, não toca em nenhuma rota de envio/WhatsApp existente; ligar como atalho dentro do chat fica pra etapa futura. Variáveis livres (`{cliente}` etc.), sem parser (2026-09-08) |
| Notificações automáticas pro cliente | CRIAR | PENDENTE |
| NPS | confirmado específico Electrolux → fica na Área 08 | N/A |
| Automação (regra geral) | CRIAR (arquitetura só, sem pressa) | PENDENTE |

## Área 08 — Integrações

| Item | Classificação | Status |
|---|---|---|
| Card de status (WhatsApp/Electrolux) | REAPROVEITAR | IMPLEMENTADO |
| Última sincronização / logs / config detalhada | REAPROVEITAR (exposição) | **IMPLEMENTADO** (2026-09-08) -- `electrolux_connections.last_sync_at`/`last_sync_error` já existiam e já eram gravados pelo fluxo real, só faltava expor. Achado de segurança: essa tabela guarda `credential_secret_name` e tem RLS SEM NENHUMA policy de SELECT (endurecimento proposital de sessão anterior) -- por isso, em vez de criar uma policy de leitura direta (exporia a credencial), foi criada a RPC `get_electrolux_sync_status` (migration `20260908190000`, `security definer`, valida `p_company_id = current_company_id()` antes de tudo, devolve só última sincronização + erro + contagem, nunca a credencial). Card "INTEGRAÇÕES" (`settings-integrations-v0907.js`) agora mostra data da última sincronização e badge "COM ERRO" quando aplicável. |
| Pulse IA (`integrated_apps`+`app_launch_audit`) | REAPROVEITAR — modelo arquitetural pras outras | IMPLEMENTADO |
| GestãoClick / Digisac | CRIAR | PENDENTE |
| Whirlpool | REVISAR — pertence à Área 02 (documento), não é integração de API | EM DIAGNÓSTICO |

## Área 09 — Sistema & Segurança

| Item | Classificação | Status |
|---|---|---|
| Tempo de inatividade / encerramento automático | REAPROVEITAR + CRIAR (exposição) | **IMPLEMENTADO** (2026-09-08, corrigido/fechado 2026-09-09) -- correção de achado: a nota anterior "nenhum monitor de inatividade implementado ainda" estava ERRADA -- `app.js` (`resetIdle()`) já tinha um monitor real, só hardcoded em 30 minutos (`renderUsers()` até documentava isso: "Bloqueio automático da interface após 30 minutos sem atividade"). `companies.session_idle_timeout_minutes` (cadastro, migration `20260908150000`) agora é lido de verdade em `access-history-v0909.js` (`applyIdleTimeout()`, chamado a cada `loadProfile()`) e aplicado em `window.vxIdleTimeoutMs`, que `resetIdle()` passou a ler no lugar do literal 30 min (fallback 30 min se a empresa não configurar nada -- comportamento atual preservado). Tela própria `settings-security-v0908.js`. Nota de navegação: RESET MASTER continua montado em `.vx-admin-actions` (tela 'admin', REAPROVEITAR) -- agora acessado pelo card "EMPRESA & USUÁRIOS", não mais por "SISTEMA & SEGURANÇA"; função em si intocada. |
| Auditoria | CONSOLIDAR — `audit_log` existe, zero escritores | **IMPLEMENTADO parcial** (2026-09-08) -- achado: não era "zero escritores" (ver C2) -- `admin_soft_delete_user`/`admin_update_user_access(_company_only)`/`master_reset_test_environment` já gravavam. O que faltava mesmo: nenhuma alteração de dado da EMPRESA (razão social, CNPJ, endereço, os parâmetros novos desta sessão) ficava registrada. Resolvido na fonte única: trigger `trg_audit_log_companies_change` (migration `20260908160000`, função `audit_log_companies_change`, security definer) -- captura QUALQUER UPDATE em `companies`, não importa se veio de RPC ou PATCH direto, não pode ser esquecida numa tela nova. Card "AUDITORIA -- ÚLTIMOS 20 REGISTROS" em `settings-security-v0908.js` (leitura, `audit_log_company` RLS já existente). Cobertura ainda parcial -- estender pra outras tabelas (ex.: `service_orders`) fica pra depois, avaliando ruído x valor (já tem `os_status_history` próprio). |
| Logs técnicos | CRIAR | **IMPLEMENTADO** (2026-09-08) -- `technical_logs` (migration `20260908170000`, insert-only pelo próprio usuário, leitura só gestor). Captura nova `client-error-log-v0908.js`: `window.addEventListener('error'/'unhandledrejection')`, grava mensagem+stack+URL, limite de 20 registros por carregamento de página (evita loop erro→erro), nunca lança exceção própria. Só front-end -- erro de servidor/Postgres fica fora do escopo. Card "LOGS TÉCNICOS -- ÚLTIMOS 20" em `settings-security-v0908.js`. |
| Reset Master | REAPROVEITAR | IMPLEMENTADO -- continua na tela 'admin' (`.vx-admin-actions`), agora acessado pelo card "EMPRESA & USUÁRIOS" no hub (o card "SISTEMA & SEGURANÇA" passou a apontar pra `settings-security-v0908.js`) |

---

## Log de execução

- **2026-09-08**: Matriz criada a partir do diagnóstico de 5 agentes.
  - C1 (aviso falso "Não existe Loja") -- **corrigido**.
  - C2 (2 RPCs sem migration versionada) -- extraídas e documentadas retroativamente.
  - C3 (catálogo de permissão divergente) -- levantamento em produção mostrou zero dado real usando as chaves conflitantes; `user-access-management-v0813.js` corrigido pra usar o catálogo canônico. Falta só endurecer a RPC contra chave arbitrária.
  - Ao iniciar construção do catálogo de produtos (Área 03), migration própria abortou com erro real (`product_types` já existe) -- achado maior: `product_groups`+`product_types` já existem no banco, populados, globais (sem company_id), nunca ligados a nenhuma tela. Rollback automático, nada ficou pela metade. Decisão de escopo (global × por empresa) posta ao usuário antes de construir a UI de gestão.
  - Leva seguinte: Numeração + Campos obrigatórios por etapa (Área 02, exposição só-leitura, sem lógica nova) e Mensagens padrão (Área 07, migration `20260908100000`, primeira tela real da Comunicação -- hub saiu de placeholder/'admin' pra 'comunicacao'). **Total: 20 itens implementados de 45.**
  - Leva seguinte: Fabricantes/garantia-reembolso (Área 05, migration `20260908110000`, card em `settings-stock-v0908.js`). **Total: 21 itens implementados de 45.**
  - Leva seguinte: Parâmetros financeiros -- juros/multa/arredondamento (Área 06, migration `20260908120000`, colunas em `companies`, card em `settings-payment-methods-v0908.js`). **Total: 22 itens implementados de 45.**
  - Leva seguinte: Parcelamento configurável -- limite de parcelas por forma de pagamento (Área 06, migration `20260908130000`, RPC estendida retrocompatível). **Total: 23 itens implementados de 45.**
  - Leva seguinte: Descontos -- limite por perfil GESTOR/ATENDENTE/TECNICO (Área 06, migration `20260908140000`). **Total: 24 itens implementados de 45.**
  - Leva seguinte: Tempo de inatividade (Área 09, migration `20260908150000`, primeira tela real de Sistema & Segurança -- hub saiu de 'admin' pra 'seguranca'; RESET MASTER permanece intocado na tela 'admin', agora só acessado por outro card do hub). **Total: 25 itens implementados de 45.**
  - Leva seguinte: Auditoria -- trigger de banco pra qualquer UPDATE em `companies` (Área 09, migration `20260908160000`), card de leitura em `settings-security-v0908.js`. **Total: 26 itens implementados de 45.**
  - Leva seguinte: Logs técnicos -- captura de erro JS não tratado (Área 09, migration `20260908170000`, `client-error-log-v0908.js`). **Total: 27 itens implementados de 45.**
  - Leva seguinte: Alertas de estoque baixo -- limite por peça + padrão da empresa + contagem real (Área 05, migration `20260908180000`, card em `settings-stock-v0908.js`). **Total: 28 itens implementados de 45.**
  - Leva seguinte: Última sincronização Electrolux (Área 08, migration `20260908190000`) -- achado de segurança no caminho: `electrolux_connections` guarda credencial e tem RLS sem policy nenhuma (endurecimento proposital); resolvido com RPC que devolve só os campos seguros, não com policy de tabela. **Total: 29 itens implementados de 45.**
  - Leva seguinte: Horário de atendimento (Área 07) -- consolidação por referência, sem duplicar `business_hours`/Área 04. **Total: 30 itens implementados de 45.**
  - C3 fechado (não conta nos 45, é conflito estrutural à parte): endurecimento final da RPC de permissão (migration `20260908200000`) -- achado no caminho: a tela canônica usa 24 chaves reais, 7 a mais que o recorte visual; catálogo travado foi ajustado pra união das 24 (checado antes de aplicar, zero dado existente ficaria fora).
- **2026-09-09**: Usuário respondeu às 16 decisões pendentes (ver artifact publicado). Execução por lotes, sem nova rodada de bloqueio -- só interromper em decisão estrutural real.
  - Lote 1 (fechamentos rápidos, sem risco): Tipo de atendimento (sem 3º valor, spec original corrigida), Períodos de agenda (mantidos os 2 valores hoje sem uso, reservados pra evolução), Importação/exportação (reclassificado de Área 09 pra Área 02), Perfis (mantidos os 5 atuais, sem 6º perfil), Regras de recebimento (validado que a regra "sem inconsistência financeira" já está implementada desde 2026-09-07), Bloqueio/desbloqueio (reaproveita `active`, rótulo do checkbox em `user-access-management-v0813.js` trocado pra deixar a ação clara). Termos e Condições/Documentos da OS: plano completo aprovado, Fase 1 iniciando.
  - Lote 2: **C4 fechado**. Investigação comparativa dos 4 arquivos confirmou `company-selector-singleton-v0813.js` como canônico (já era o único visível na prática, por ordem de carregamento + `hideAllLegacy()`). Removido o código morto dos outros 3 (`company-only-mode-v0813.js`, `user-logoff-v0813.js`, `user-permissions-ui-v0813.js`), exposto `window.vxRefreshCompanySelector` pra sincronizar o único seletor real quando a troca de empresa acontece por outro caminho -- corrige de quebra um bug latente de rótulo desatualizado. Botões de logout/SAIR de cada arquivo preservados intocados (fora do escopo do C4).
  - Lote 3: **Técnicos -- região/especialidade/disponibilidade** (Área 01/04). 3 tabelas novas + 3 RPCs (migration `20260909010000`), especialidade reaproveita `product_types` (sem dicionário novo, decisão explícita do usuário). Card com modal por técnico em `settings-schedule-v0907.js`. **Total: 31 itens implementados de 45.**
  - Lote 4: **Histórico de login/logout** (Área 01) -- `access_history` (migration `20260909020000`), interceptado via `window.auth`/`window.loadProfile`, arquivo novo `access-history-v0909.js`. **Total: 32 itens implementados de 45.** No mesmo lote, achado + correção: "Tempo de inatividade" (Área 09, já implementado em 2026-09-08) tinha a nota errada "nenhum monitor implementado" -- na verdade `app.js` já rodava um timer real de 30 min hardcoded; ligado o parâmetro `session_idle_timeout_minutes` a esse timer real (`window.vxIdleTimeoutMs`), fechando o item de verdade (não conta como um novo item dos 45, é correção/fechamento de um já contado).
  - Lote 5: **Alertas de Agenda** (Área 04). Único caso genuinamente faltante era conflito/sobreposição de agendamento -- `hasConflict()` nova em `field-agenda-complete-v0813.js`, mesmo padrão não-bloqueante de `isBlocked`. Achado extra: `openNewForOrder` nem checava bloqueio/feriado -- corrigido junto. Os outros 3 casos pedidos (técnico indisponível, sem técnico, card de atenção) já existiam de verdade, confirmados por leitura do código antes de reportar como prontos. **Total: 33 itens implementados de 45.**
  - **C5 fechado** (não conta nos 45): consolidadas as 3 telas concorrentes de "Alterar Usuário" -- achado igual ao C4 (interceptador de clique em fase de captura decidindo o vencedor de fato), capacidade real portada (Empresas Liberadas, via Edge Function `voxassist-manage-user` já existente) antes de remover as outras 2.
  - **C6 fechado** (não conta nos 45): consolidados os 2 formulários de empresa -- mesma classe do C5, `company-profile-complete-v0812.js` já vencia na prática por captura de clique; formalizado com `window.vxEditCompanyFull` explícito.
  - Investigação do Pulse IA (app externo, repositório próprio `jeffersonadmrocks-web/pulse-ia`, fora do VoxAssist): confirmado que a "API única pra qualquer IA" que o usuário queria aprovar já existe (`pulse-ingest` + `/api/mcp`, 3 ferramentas). Achado registrado lá, não aqui -- fora do escopo desta Matriz.
  - Lote 6: **Segurança -- redefinir senha + encerrar sessão remota** (Área 01). Redefinir senha reaproveita o endpoint público `/auth/v1/recover` (confirmado via código-fonte do `@supabase/auth-js`, zero Edge Function). Encerrar sessão remota é RPC nova (`admin_terminate_user_sessions`, migration `20260909030000`) que apaga `auth.sessions` diretamente -- confirmado que a API pronta do Supabase (`admin.signOut`) exigiria o JWT da sessão-alvo, inviável pro gestor; acesso de leitura à tabela testado antes de aplicar. **Total: 35 itens implementados de 45.**
